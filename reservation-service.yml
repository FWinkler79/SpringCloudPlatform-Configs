# Should be managed in the service's bootstrap.yml
# since the service needs to be able to start without
# the config-server being up and running.
# Besides: with services deployed to the Cloud inside
# individual containers, each service's (internal) port
# will always be 8080. Changing the port is only for local
# development.
# server:
#  port: ${PORT:2222}

com.equalities:
  greeting: "Hello LOCAL World!"